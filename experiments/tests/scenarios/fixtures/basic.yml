name: basic-room
seed: 42

agents:
  - id: alice
    behavior: mentioned

  - id: bob
    behavior: mentioned

  - id: charlie
    behavior: mentioned

rooms:
  - id: main
    members:
      - alice
      - bob
      - charlie

scheduler:
  type: highest_urgency

steps: 10
